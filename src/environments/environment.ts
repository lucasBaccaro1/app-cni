// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  firebase: {
    projectId: 'app-cni-produccion',
    appId: '1:988325500428:web:860bad8fab5f1550ec4029',
    storageBucket: 'app-cni-produccion.appspot.com',
    apiKey: 'AIzaSyDCCPnpEvJdWJMmgpoSyvnwGmb-ca86R-w',
    authDomain: 'app-cni-produccion.firebaseapp.com',
    messagingSenderId: '988325500428',
  },
  production: false
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
